# 启动指南（异地两人 · 不同 WiFi / 5G）

> 技术部分（代码 / 端到端加密 / 仅内存中继）已全部就绪，中继转发链路已通过实测（RESULT: PASS）。  
> 你选定的部署方案：**方案 B（Railway 免费托管）**。  
> AppID 已填好：`wxa1e44502c459abda`（已写入 `project.config.json`）。

---

## 一、电脑上准备小程序（你本机）

- [x] 1\. 装「微信开发者工具」（微信官网下载稳定版）
- [x] 2\. 开发者工具 → 导入项目 → 选**本目录**（project.config.json 已含正确 AppID）
- [x] 3\. 详情 → 本地设置 → 勾选「不校验合法域名 / TLS」✔（开发期用；正式配好白名单后可取消）
- [x] 4\. 工具 → **构建 npm（现在可跳过）**：加密已改为本地内联实现（`utils/tweetnacl.js`），不再依赖 npm 包，所以**不用构建 npm 也能跑**。之前那个 `module 'tweetnacl/crypto.js' is not defined` 报错正是 npm 版在小程序里用不了 Node 的 crypto，现已根除。
- [x] 5\. 编译 → 模拟器看到**真能播放的短视频流**页面 ✅（上下滑动切换、单击播放/暂停；功能已可验证）

## 二、部署公网中继（方案 B：Railway，零域名零证书）

前置：一个 GitHub 账号 + 一个 Railway.app 账号（可用 GitHub 直接登录）。

- [ ] 1\. 把整个项目推到你的 GitHub 仓库（保证 `relay/` 子目录在里面）
- [ ] 2\. 打开 railway.app → **New Project** → **Deploy from GitHub repo** → 选该仓库
- [ ] 3\. 部署设置里把 **Root Directory 设为 `relay`**（关键！否则会误用小程序根目录的 package.json）
- [ ] 4\. 点 Deploy。Railway 会读 `relay/railway.json` + `relay/Dockerfile`，自动构建并启动
- [ ] 5\. 部署完成 → 进项目 → **Settings** → 复制平台给的默认域名，形如 `xxx.up.railway.app`
- [ ] 6\. 记下来：`wss://xxx.up.railway.app`
- [ ] 7\. 同页 **Variables** → 加一条 `PEXELS_API_KEY=你的key`（免费申请：<https://www.pexels.com/api/，注册即得，立即可用）。\\\*\\\*不设也能跑\\\*\\\*，只是回退到内置测试视频；设了就变成无限竖屏真实短视频流>

> 备选 CLI 方式（本机已装 node）：`npm i -g railway` → `railway login` → 在 `relay/` 目录内 `railway init` → `railway up`。  
> Railway 有免费额度，注册后按网页提示激活即可；长期高频使用建议自有服务器（见第五节）。

## 三、小程序后台配合法域名（真机必须）

中继默认**代转视频流**（`/vproxy`），所以视频和小程序请求都走你 Railway 这一个域名：

- [ ] 1\. mp.weixin.qq.com → 开发 → 开发设置 → 服务器域名
- [ ] 2\. 添加 **socket 合法域名**：`wss://xxx.up.railway.app`
- [ ] 3\. 添加 **request 合法域名**：`https://xxx.up.railway.app`（小程序拉 `/feed` 用）
- [ ] 4\. 添加 **downloadFile 合法域名**：`https://xxx.up.railway.app`（`/vproxy` 视频流走这个）
- [ ] 5\. 保存（个人小程序允许配置，无需审核即在体验版生效）

> 追求更快、想省 Railway 流量？在 Railway 把 `FEED_PROXY` 设成 `0`：视频改走 Pexels 直链，此时还要额外在 downloadFile 合法域名里加 `vod-progressive.akamaized.net`、`videos.pexels.com`、`images.pexels.com`、`player.vimeo.com`。默认开代转最省事，只白名单一个域名即可。

## 四、你和老婆真机设置

- [ ] 1\. 你：开发者工具 → 真机调试 → 扫码（你手机）
- [ ] 2\. 老婆：mp.weixin.qq.com → 用户管理 → 体验成员 → 添加她微信号 → 她开「体验版」小程序
- [ ] 3\. 两人各点左下 🔑 →「复制我的公钥」互发 →「设置对方公钥」粘入
- [ ] 4\. 两人 🔑 →「设置房间号」，填**相同**字符串
- [ ] 5\. 两人 🔑 →「设置中继地址」，填 `wss://xxx.up.railway.app`
- [ ] 6\. 聊天输入 → 点「发送」→ 异地实时加密私聊 ✅（不同网络也能通）

## 五、方案 A（自有服务器）备选

若以后想更稳 / 长期，见 `relay/docker-compose.yml` + `relay/Caddyfile`：一台公网服务器 + 域名，进入 `relay/` 执行 `docker compose up -d`，对外即 `wss://你的域名`（Caddy 自动签发证书）。

---

## 手势速记

- 左上 1/3 **长按** = 进聊天；按住**下滑到下半屏** = 固定；聊天里点**上方 1/3** 或右上「返回」= 退出
- **切后台 / 锁屏再进** = 回浏览页
- 浏览页**单击视频** = 播放/暂停；**双击视频** = 假"加载失败"（掩饰误触）；**长按视频** = 假"崩溃"，点关闭可真退出
- 视频源：默认接入 **Pexels 公开竖屏短视频 feed（经中继代理，可无限刷）**；划到底自动加载下一页。无 Pexels 密钥时回退内置测试视频。想私有在 🔑 → 设置视频列表 粘贴你的 mp4，或把视频放进 `relay/videos/` 走你自己的服务器；🔑 第 6 项「恢复公开视频流」可切回 feed

## 已知限制 / 安全提醒

- 小程序名称 / 图标 / 微信框架始终可见，非原生 App 式彻底隐身
- 中继**仅内存**缓冲最近 10 分钟 / 50 条，双方同时离线超窗口会丢（符合"不落盘"约定）
- Railway 免费层有额度 / 休眠限制，长期高频建议自有服务器（方案 A）
- 请在你有合法权利隐私保护的场景使用，遵守《个人信息保护法》与微信规则
